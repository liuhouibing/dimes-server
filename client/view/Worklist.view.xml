<mvc:View
	controllerName="sap.ui.demo.bulletinboard.controller.Worklist"
	xmlns="sap.m"
 	xmlns:app="http://schemas.sap.com/sapui5/extension/sap.ui.core.CustomData/1"
	xmlns:mvc="sap.ui.core.mvc"
	xmlns:semantic="sap.m.semantic">
	<semantic:FullscreenPage
		id="page"
		>
		<semantic:content>
			<Table
				id="table"
				width="auto"
				class="sapUiResponsiveMargin"				
				growing="true"
				items="{
					path: '/Posts',
					sorter: {
						path: 'Title',
						descending: false
						}
					}"
				busyIndicatorDelay="{worklistView>/tableBusyDelay}"
				updateFinished=".onUpdateFinished">
				<headerToolbar>
					<Toolbar>
												
						<Button text="新增" icon="sap-icon://create" press="onPostForm"  >
							<layoutData>
									<ToolbarLayoutData shrinkable="true" />
							</layoutData>
						</Button>

						<ToolbarSpacer />
						<ToolbarSpacer />
						<SearchField id="searchField" width="auto" search=".onFilterPosts"  placeholder="Empty SearchBox to Reset" />
					</Toolbar>
				</headerToolbar>
				<columns>
					<Column width="33%" id="nameColumn" vAlign="Middle">
						<Text text="{i18n>TableNameColumnTitle}" id="nameColumnTitle"/>
					</Column>
					<Column width="33%" id="categoryColumn" vAlign="Middle" >
						<Text text="{i18n>TableCategoryColumnTitle}" id="categoryColumnTitle"/>
					</Column>
					<Column width="33%" id="unitNumberColumn" hAlign="End" vAlign="Middle">
						<Text text="{i18n>TableUnitNumberColumnTitle}" id="unitNumberColumnTitle"/>
					</Column>
					<Column width="80px" id="flaggedColumn" demandPopin="true" vAlign="Middle"/>
				</columns>
				<items >
					<ColumnListItem
						vAlign="Middle"
						type="Navigation"
						press=".onPress">
						<cells>
							<ObjectIdentifier
								title="{Title}"/>
							<Text
								text="{Category}" />
							<ObjectNumber
								number="{
										path: 'Price',
										formatter: '.formatter.numberUnit'
									}"
								state="{
										path: 'Price',
										formatter: '.formatter.priceState'
									}"
								unit="{/Currency}"/>
							<ToggleButton
								id="flaggedButton"
								tooltip="{i18n>flaggedTooltip}"
								icon="sap-icon://decline"
								visible = "{= ${Flagged}  > 0 ? true : false }"
								enabled = "false"
								pressed="{
										path: 'Flagged',
										type: '.types.flagged'
									}"
								
								press="handleToggleButtonPress"
								app:PostID = "{/PostID}"/>
						</cells>
					</ColumnListItem>
				</items>
			</Table>
		</semantic:content>				
			
		<semantic:customFooterContent >
		<Label id="lblCurrentUser" text="{worklistView>/worklistCurrentUser}" />
		<Label id="lblSeparator" text=" | " />
		<Label id="tableHeader" text="{worklistView>/worklistTableTitle}" />		
		<Button id="menu" visible="true" text="" enabled="true"	icon="sap-icon://menu"	press="handlePressOpenMenu">
		</Button>
		</semantic:customFooterContent>
	</semantic:FullscreenPage>
</mvc:View>
